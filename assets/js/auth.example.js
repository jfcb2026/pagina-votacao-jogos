/* auth.example.js — modelo/exemplo. Copia este ficheiro para auth.js
   (que fica fora do repositório, ver .gitignore) e define aí a
   password inicial de acesso ao site. */

const SITE_PASSWORD = "muda-me";
